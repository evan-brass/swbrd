[package]
name = "socket3"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.189"
nix = { version = "0.31.3", features = ["socket", "ioctl"] }
socket2 = { version = "0.6.5", features = ["all"] }
