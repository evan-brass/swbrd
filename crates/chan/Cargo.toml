[package]
name = "chan"
version = "0.1.0"
edition = "2024"
license = "MIT-0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zerocopy = { version = "0.8.50", features = ["derive"] }
